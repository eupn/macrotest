language: rust
rust:
  - stable
  - beta
  - nightly
jobs:
  allow_failures:
    - rust: nightly
  fast_finish: true

script:
  - cargo build --verbose --all
  - cd test-project
  - cargo test -- --nocapture
  - cd ../test-procmacro-project
  - cargo test -- --nocapture